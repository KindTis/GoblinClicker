# Goblin Clicker MVP Assets

MVP ships with simple local SVG assets for goblin states, feedback effects, currency, upgrades, and background. Runtime code references assets through `assetManifest.ts` keys rather than hard-coded paths.
